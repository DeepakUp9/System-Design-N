# Factory Design pattern  or factory method Design pattern
  when there is superclass and multiple subclasses and we want to get object of subclasses based on 
  input and requirement. Then we create Factory class which take the responsibility of creating object of the class based on input 

  ## Advantages of factory Design pattern
    1. Focus on creating object for interface rather than implementation.
    2. Loose coupling. more robust code.
